using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class nonrecyccle : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        cube.recycleOrNot = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
